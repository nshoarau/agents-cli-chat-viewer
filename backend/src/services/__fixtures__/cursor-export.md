# Cursor Export: Support session imports

## User
Please add support for exported Cursor chats in Markdown form.

## Assistant
I can parse exported Markdown by splitting `User` and `Assistant` sections into messages.

## User
Make sure it still works when the file is added as a custom watch folder.

## Cursor
That path will be treated as a Cursor transcript even without the native local database.
