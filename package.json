{
  "name": "agents-cli-chat-viewer",
  "version": "1.0.0",
  "description": "",
  "main": "eslint.config.js",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "servers": "npm run dev",
    "test:e2e": "npm run test:e2e --prefix frontend",
    "test": "npm test --prefix backend -- --run --passWithNoTests && npm test --prefix frontend -- --run --passWithNoTests",
    "lint": "npm run lint --prefix backend && npm run lint --prefix frontend"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
